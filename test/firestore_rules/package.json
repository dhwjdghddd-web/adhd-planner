{
  "name": "adhd-planner-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Emulator-based regression tests for firestore.rules data isolation.",
  "scripts": {
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase": "^10.0.0",
    "jest": "^29.0.0"
  }
}
